# 长期记忆型 AI 助理 —— 任务 2：记忆与调用机制设计说明

> 本文说明记忆从**记录 → 组织 → 调用 → 纠正 → 衰减**的完整运作机制。设计前提是任务 1 的结论：产品定位为"带出处、可纠错、主动调用的工作记忆层"，产品价值 = 召回相关度 × 准确率 × 可溯源性，三者乘法关系。以下每个机制都对应这条公式中的至少一个因子。

---

## 一、设计目标与原则

**目标**：让用户敢在"不核对"的前提下采信记忆输出，同时在出错时能一键核对、一键纠正、错误不扩散。

**四条设计原则：**

1. **记忆即引用**——任何一条记忆都不是孤立的事实陈述，而是"内容 + 出处 + 时间 + 置信度"的结构化记录。没有出处的记忆不允许入库。
2. **写入端分级设防**——高置信自动记，低置信/高影响先确认。写入端省的事，会在纠正端加倍还回来。
3. **调用必须声明**——助理引用记忆时显式告知"我在引用哪次记录"，用户始终能区分"记忆"与"模型当下推理"。
4. **纠正闭环可及**——错误从被发现到被修复，路径不超过两步；且修复能回溯影响范围。

---

## 二、记忆什么：记忆的分层与分类

不是所有对话内容都值得记住。记忆分三层，越往上越稳定、影响面越大、写入校验越严：

| 层级 | 内容 | 示例 | 稳定性 |
|---|---|---|---|
| **个人画像** | 角色、偏好、工作习惯、常用口径 | "用户偏好结论先行的汇报结构" | 长期稳定，缓慢演化 |
| **项目记忆** | 项目背景、目标、关键决策、当前阶段、结论 | "X 项目 Q3 目标是完成竞品对标，已排除方案 B" | 随项目演进更新 |
| **会话工作记忆** | 当前对话内的临时上下文 | 本次讨论的中间结论 | 会话结束即评估：沉淀为项目记忆或丢弃 |

**不记的东西**：闲聊、一次性的临时指令、用户明确要求不记的内容、无法定位出处的转述。

**与文档知识库的关系**：文档库是"外部事实"的来源（机构沉淀的报告、资料），记忆层是"对话与协作中产生的认知"（决策理由、偏好、过程结论）。两者分开存储、统一检索——助理回答时可能同时引用一份文档和一条记忆，出处标注会区分来源类型。

---

## 三、怎么记：混合制写入机制

写入采用**置信度分流**：系统自动判断每条候选记忆的处理路径。

### 1. 候选记忆的生成

对话进行中，系统持续从对话流中抽取"值得长期保留"的候选：决策、结论、背景信息、偏好表达、项目状态变化。抽取时同步生成元数据：

- **内容**：规范化后的事实陈述（"X 项目的交付日期定为 10 月 15 日"）
- **出处**：具体到哪次对话、哪条消息（对话 ID + 消息定位）
- **时间**：记录时间 + 事实所指时间（"上周定下的" → 解析为具体日期）
- **置信度**：高 / 中 / 低（依据：用户是否明确陈述、是否有歧义、是否与既有记忆冲突）
- **影响面**：仅当前项目 / 跨项目 / 个人画像级
- **状态**：生效中 / 待确认 / 已纠正 / 已作废

### 2. 分流规则

| 候选特征 | 处理方式 | 示例 |
|---|---|---|
| 高置信 + 低影响（用户明确陈述的单项目事实） | **自动入库**，对话中轻提示："已记下：X 项目交付日期 10/15" | 用户说"这个项目 deadline 定在 10 月 15 号" |
| 高置信 + 高影响（个人画像、跨项目结论） | **生成候选，轻量确认**：对话末尾或卡片中一键确认/修改 | 系统推断"用户似乎偏好数据先行"，需确认后写入画像 |
| 中低置信（推断所得、表述模糊） | **显式确认**："我理解你的意思是……，要记下来吗？" | 从上下文推断的项目优先级 |
| 与既有记忆冲突 | **冲突提示**："这和你 9 月 3 日说的'……'不一致，以哪条为准？" | 交付日期从 10/15 改为 10/30 |

冲突检测是写入质量的关键闸口：**新事实不是追加，而是触发对旧记忆的裁决**——覆盖、并存（不同项目各自成立）或待用户裁决。

### 3. 用户侧的写入入口

除系统自动抽取外，用户始终可以：

- 对话中显式要求："**这个你记着**"——最高优先级写入；
- 对话中说"**这个不用记**"或"**把刚才那条忘掉**"——阻止写入或即时删除；
- 在**记忆管理页**统一查看、编辑、删除全部记忆与知识库内容（按项目/类型/时间筛选），写入和修改在页面同样留痕。

### 4. 冷启动：存量历史导入

平台可读取用户的历史对话、偏好与项目记录——产品上线第一天不必从空白开始。冷启动流程：

1. **批量导入**：系统扫描用户的历史对话与项目记录，批量生成候选记忆（全部标记为"冷启动来源"，置信度从低起步）；
2. **集中确认**：候选记忆在记忆管理页以清单形式呈现（按项目分组），用户一次性批量确认/修改/删除。**冷启动的记忆一律不自动生效**——历史对话缺乏当下语境，误判率高，宁可多确认一轮；
3. **信任预热**：首次使用的头两周，系统提高显式确认比例、降低主动唤起频率，让用户先看到"它记得对"，再逐步放手。

冷启动是信任建立的关键期：用户第一次打开就看到助理"已经懂我的项目"，价值感知极强；但如果首批记忆有错，信任崩塌也发生在这里——所以确认闸口在冷启动阶段收得最紧。

---

## 四、怎么存：记忆条目的组织

每条记忆是一个**带版本的结构化条目**，而不是一段自由文本：

```
记忆条目
├── 内容（当前生效版本）
├── 版本历史（每次修改的旧版本 + 修改原因）
├── 出处（来源对话/文档 + 定位）
├── 时间（记录时间、事实时间、最近验证时间）
├── 置信度与状态
├── 归属（项目 / 个人画像）
├── 密级与隔离（普通 / 项目隔离——不允许跨项目唤起）
└── 关联（被哪些后续结论引用过）
```

两个要点：

- **版本历史不可省略**。纠正不是覆盖旧值，而是追加新版本并标记旧版本作废——这是"难以追溯是哪次记录出了问题"的直接解法：任何问题都能沿版本链定位到出错的那次记录。
- **关联字段记录"被谁引用过"**。一条记忆被后续 5 次分析引用过，纠正它时系统知道影响面，这是第六节纠正机制的基础。

---

## 五、怎么用：主动唤起 + 显式出处

### 1. 唤起时机

系统在三种时机主动检索记忆：

1. **对话开场**：识别当前对话所属项目（或无法判定时询问），自动加载该项目的记忆摘要作为背景；
2. **话题命中**：当前讨论内容与某条记忆相关度超过阈值时唤起——包括**跨项目命中**（"这个主题你在 A 项目调研过"），这是跨项目复用价值的实现点；
3. **冲突预警**：用户当前陈述与既有记忆矛盾时，唤起旧记忆进行确认（与写入端冲突检测呼应）。

### 2. 引用时的显式声明

助理引用记忆的输出必须满足两条：

- **可分辨**：引用记忆的内容带有可见标记（如引用卡片、角标），与模型当下生成的内容视觉上可区分；
- **可追溯**：每个引用标记可展开出处——"来自 9 月 3 日关于 X 项目的对话"或"来自文档《XX 调研》第 3 节"，点击可跳转原文核对。

**示例形态：**

> X 项目的目标用户是二线城市中小企业主 📎 *（记忆 · 9 月 3 日对话）*，因此在渠道选择上，结合你这次的预算约束，我建议……

前半句是记忆（有出处、可核对），后半句是当下推理（无标记）。用户一眼看清边界。

### 3. 置信度的呈现

低置信或久未验证的记忆被引用时，助理主动降调表述："我此前记录的是……（3 个月前），现在是否仍然成立？"——把"可能过时"显性化，而不是用同样确信的语气输出一条旧记忆。

### 4. 跨项目唤起的隔离与确认（机构场景的合规闸口）

机构的分析师通常背负客户保密义务，"跨项目复用"在这里既是价值点也是事故点。机制上设三道闸：

1. **密级标记**：项目可被标记为"隔离"（涉密客户、保密协议期内的项目）。隔离项目的记忆**不参与跨项目唤起**，仅在项目内可用；密级由机构管理员或项目负责人在项目设置中控制；
2. **跨项目唤起显式确认**：普通项目之间发生跨项目命中时，助理不明示具体内容，先询问："你在 A 项目中调研过类似主题，要调出来参考吗？"——用户确认后才引入，且引用时来源项目始终可见；
3. **输出侧拦截**：当生成内容流向外部交付物（导出、报告）时，系统检查其中的记忆引用是否跨项目来源，若是则提示用户复核，防止客户信息随交付物外泄。

### 5. 误唤起的成本控制

主动唤起的反面是打扰。三条控制线：

- **相关度阈值**：低于阈值不唤起；阈值随用户使用数据个性化调整，宁可少唤起，不错唤起；
- **弱展示分层**：中等相关的记忆不强插进回答，仅在侧边以"可能相关"弱提示出现，由用户决定是否采纳；
- **反馈学习**：用户对任何唤起可一键反馈"这条没用"，反馈直接影响后续唤起排序——误唤起率本身是被监控的核心指标（目标 < 15%，见任务 1 的量化验收指标）。

---

## 六、怎么改：纠正与溯源闭环

这是案例风险的正面回应。纠正设计的目标：**两步内完成修复，修复可回溯影响面，纠错不损伤信任。**

### 1. 对话内即时纠正（主路径，一步）

用户在对话中指出错误："不对，交付日期改成 10 月 30 号了。"系统执行：

1. **定位**：找到被引用的那条记忆（因为引用带出处，定位是精确的，不是模糊搜索）；
2. **修复**：记忆条目追加新版本，旧版本标记"已纠正"，保留版本历史；
3. **影响提示**：若该记忆有"被引用"关联记录，告知用户："这条记录还在 9 月 5 日的渠道分析中被引用过，相关结论可能需要复核"——**错误扩散到哪里，就提示到哪里**；
4. **确认**：一句话反馈修复结果，不打断当前工作流。

### 2. 记忆管理页统一纠正（第二入口）

用户在记忆/知识库管理页浏览时，可直接编辑或删除任何条目。所有修改留痕（谁改的、什么时候、从什么改成什么），与对话内纠正走同一套版本机制。

### 3. 溯源查询（问题发生后）

当用户发现某次输出有问题、想查"是哪次记录出了问题"时：

- 从输出沿引用标记 → 定位记忆条目 → 查看版本历史与出处 → 找到出错的那次记录（是写入时就错了，还是后来被改错的）；
- 从记忆条目沿"被引用"关联 → 看到所有受影响的下游输出。

两个方向都通，案例中的"难以追溯"被结构性消除。

### 4. 纠错与信任的关系

纠错机制本身也是信任建设：用户每成功纠正一次并看到"已修复 + 影响范围已提示"，对系统的信任是**上升**的——因为他验证了"出错有救"。这与任务 1 的成立信号（"纠错后信任不降级"）直接对应。

---

## 七、怎么忘：时效与衰减

长期记忆不等于永久记忆。机制上处理三类"该忘"：

1. **事实过期**：带明显时效的记忆（项目阶段、进度）超过设定周期未被验证，引用时自动降调（见第五节），并在管理页标记"待复核"；
2. **项目归档**：项目结束后，其记忆整体转为归档态——不再主动唤起，但可被检索和显式引用；归档项目若重启，一键激活；
3. **用户删除**：任何记忆可被用户删除，删除即彻底清除（含版本历史），并向用户确认删除的影响面。

衰减机制的存在让记忆库长期保持"信噪比"——记忆越多越值钱的前提是旧的、错的、过时的能被代谢掉。

---

## 八、端到端示例：一条记忆的完整生命周期

**9 月 3 日（记录）**：用户在对话中说"X 项目我们决定主攻二线城市中小企业主"。系统判定：用户明确陈述 + 单项目事实 → 高置信低影响 → **自动入库**，轻提示"已记下"，条目携出处（本次对话）、时间、归属项目 X。

**9 月 10 日（调用）**：用户开启新对话讨论渠道策略。系统识别话题归属项目 X，唤起该记忆，输出："X 项目目标用户是二线城市中小企业主 📎（记忆 · 9 月 3 日对话），所以渠道上建议……"用户看到出处，未核对，直接采信——这正是产品要的价值时刻。

**9 月 20 日（纠正）**：用户在另一轮对话中说"客户方向变了，我们改做一线城市大型连锁"。系统检测到与既有记忆冲突 → 冲突提示："这和你 9 月 3 日确定的'二线城市中小企业主'不一致，以哪条为准？"用户确认新方向 → 条目追加新版本，旧版本作废留痕 → 系统提示："该记忆曾在 9 月 10 日的渠道策略讨论中被引用，建议复核当时结论。"

**10 月（衰减与合规唤起）**：项目 X 结束，记忆归档。三个月后用户启动类似的新项目 Y，系统跨项目命中归档记忆——但不明示内容，先询问："你在已归档的 X 项目中做过相关调研，要调出来参考吗？"用户确认后，记忆带着来源项目和归档状态标注进入对话——跨项目复用在有出处、有状态、有确认的前提下发生。（若 X 项目被标记为隔离，这次唤起根本不会发生。）

---

## 九、机制与风险的对应关系（对照案例）

| 案例暴露的风险 | 对应机制 |
|---|---|
| 用户把助理当作"记得所有历史"的对象，不再核对 | 引用显式标记 + 出处可查，核对成本降到一次点击；低置信/久未验证的记忆主动降调表述 |
| 错误记忆在后续多场景反复引用 | "被引用"关联记录 + 纠正时的影响面提示，错误扩散到哪里就提示到哪里 |
| 难以追溯是哪次记录出了问题 | 记忆条目带出处 + 版本历史不可省略，双向溯源（输出→记忆→记录，记忆→下游输出） |
| 信任一旦崩塌不可逆 | 混合制写入（错误入库前置拦截）+ 纠错闭环（出错有救的可见证据）+ 记忆管理页（用户始终握有最终控制权） |
| 机构场景下跨项目复用泄露客户保密信息 | 密级隔离标记 + 跨项目唤起先询问后引用 + 交付物导出时的跨项目引用拦截 |

**一句话总结**：这套机制把"记忆"从用户想象中的"全知黑盒"，变成一个**每一条都能点开看出处、看历史、看影响面的透明账本**——记忆的长期价值靠积累，积累的资格靠可信。
