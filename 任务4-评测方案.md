# 长期记忆型 AI 助理 —— 任务 4：评测方案

> 本文回答一个问题：**如何判断"记忆准确性"与"主动调用价值"是否达标。** 评测设计承接任务 1 的定位公式（产品价值 = 召回相关度 × 准确率 × 可溯源性）与任务 2 的机制设计——机制是承诺，评测是验证承诺兑现的手段。

---

## 一、评测目标与原则

**总目标**：验证"用户敢不敢不核对"这一成立标准，在受控环境下被持续满足。

三条原则：

1. **评的是链路，不是单点**。记忆准确性不是"模型记得对不对"一个数，而是"写入 → 存储 → 唤起 → 引用 → 纠正"全链路的逐段合格率——任一环节漏，整体信任就漏。
2. **离线评测集 + 在线试点 + 对抗注入，三层缺一不可**。离线保回归，在线保真实，对抗保底线。
3. **不达标有明确处置**，评测不是出报告，是上线门禁（gate）。

---

## 二、评测对象拆解：四个被测能力

| # | 被测能力 | 对应机制 | 核心问题 |
|---|---|---|---|
| A | **记忆写入质量** | 混合制写入、置信度分流 | 该记的记了吗？不该记的没记吧？记的内容忠实于原话吗？ |
| B | **记忆引用准确性** | 显式出处、置信度降调 | 引用的内容与被引记忆一致吗？出处标对了吗？过时的降调了吗？ |
| C | **主动调用价值** | 唤起时机、跨项目唤起、误报控制 | 唤起的东西相关吗？省去了重复交代吗？打扰多吗？ |
| D | **纠错与溯源有效性** | 版本留痕、影响面提示 | 用户纠正后修对了吗？影响面提示全了吗？还能定位到出错的那次记录吗？ |

---

## 三、三层评测方法

### 第 1 层：离线评测集（回归门禁，每次模型/prompt/策略变更必跑）

**评测集构建**：编写 30–50 个多会话剧本（scripted conversation），每个剧本模拟真实的跨天使用：

- 剧本内含：明确陈述的事实、模糊表述、闲聊噪音、前后矛盾的陈述（用于测冲突检测）、跨项目相关主题、过时信息；
- 每个剧本配**标准答案集（golden labels）**：应写入哪些记忆（内容、类型、置信度档位）、不应写入哪些、何时应触发冲突提示、何时应唤起哪条记忆、引用应标注哪个出处。

**评测执行**：剧本输入系统，比对系统行为与标准答案：

| 评测项 | 判定方式 |
|---|---|
| 写入召回率：该记的是否入库 | golden set 比对（自动 + 人工抽检语义一致性） |
| 写入精确率：不该记的是否被拦 | golden set 比对（噪音/闲聊/临时指令是否误入库） |
| 写入忠实度：记忆内容是否忠于原话 | LLM-as-judge 初判 + 人工复核争议样本（抽样 ≥20%） |
| 冲突检测：矛盾陈述是否触发裁决而非静默覆盖 | 逐条核对剧本中的埋点冲突 |
| 唤起相关性：唤起的记忆与当前话题是否相关 | 标注员二分类判定（相关/不相关） |
| 引用忠实度：输出中引用内容与记忆原文是否一致 | 逐条比对；出处 id 是否正确可自动校验 |

**达标线（上线门禁）**：

| 指标 | 达标线 | 理由 |
|---|---|---|
| 写入忠实度 | ≥ 98% | 错误记忆是复利污染源，这是语义判定类指标中最严的一档（100% 的两项是确定性校验，性质不同） |
| 冲突检测触发率 | 100%（埋点冲突全检出） | 静默覆盖旧记忆是信任崩塌的直接路径，零容忍 |
| 出处标注正确率 | 100% | 出处标错 = 溯源机制失效，零容忍 |
| 写入召回率 | ≥ 90% | 漏记损失体验，可迭代补偿 |
| 写入精确率 | ≥ 90% | 记忆库信噪比 |
| 唤起相关率 | ≥ 85% | 对应在线误报率 < 15% 的目标 |

任何一项跌破达标线 → 阻断上线，回到机制层修复，不允许"带着已知问题灰度"。

### 第 2 层：在线试点（真实价值验证，2–4 周）

选 3–5 名符合画像的分析师真实使用（与任务 1 最窄验证切口同一批人），埋点采集：

| 指标 | 达标线 | 测量方式 |
|---|---|---|
| 背景复述量（对话开头用户粘贴背景的字数/轮次） | 4 周内下降 ≥ 60% | 对话日志统计 |
| 记忆引用采纳率（用户未否定/未修改即采用） | ≥ 80% | 引用标记的后续行为 |
| 记忆错误率（被引用记忆中被纠正比例） | < 5% 且逐周下降 | 纠错事件 / 引用次数 |
| 主动唤起误报率（"这条没用"反馈占比） | < 15% | 唤起反馈埋点 |
| 纠错完成时长（指出错误 → 修复 + 影响提示） | 对话内一步完成 | 纠错事件日志 |
| 托管行为（用户主动说"这个你记着"） | 出现且逐周增长 | 写入入口统计 |
| 跨项目复用实例数 | 每周 ≥ 1 例真实发生 | 跨项目唤起且被采纳的日志 |

**定性补充（每周一次 15 分钟回访）**，问三个问题：
1. "这周有没有哪次你没交代背景、但它答对了？"（价值时刻）
2. "有没有它记错或唤起无关记忆的时候？你当时怎么处理的？"（信任磨损点）
3. "如果明天关掉记忆功能，你会损失什么？"（退路测试——答不出损失 = 价值未成立）

### 第 3 层：对抗与故障注入（底线测试，上线前 + 每季度）

主动攻击自己的系统，验证任务 1 预判的死法不会发生：

| 注入场景 | 验证点 | 通过标准 |
|---|---|---|
| **毒记忆注入**：人工向记忆库写入一条与事实不符的记忆 | 被引用时是否可溯源、可一步纠正；纠正后影响面是否完整提示 | 沿引用标记两步内定位到出错记录；影响面提示覆盖全部下游引用 |
| **矛盾注入**：在不同会话先后陈述两个矛盾事实 | 是否触发冲突裁决 | 100% 触发，无静默覆盖 |
| **过时注入**：写入带时效的记忆并快进时间 | 引用时是否降调提示时效 | 引用必然附带时效提醒 |
| **跨项目保密注入**：A 项目标记为隔离，在 B 项目讨论相关话题 | 隔离记忆是否泄漏 | 零泄漏；普通跨项目命中必先询问 |
| **诱导注入**：用户随口说的假设性表述（"假如我们做……"） | 是否被误记为决策 | 不误入库，或进入待确认且被用户拦下 |
| **纠错连环**：连续纠正同一条记忆 3 次 | 版本链是否完整、性能是否正常 | 版本历史完整可查，v1 始终可回溯 |

---

## 四、评测节奏与组织

```
每次模型/prompt/记忆策略变更 ──→ 跑离线评测集（回归门禁，不达标不上线）
上线前 ──────────────────────→ 对抗注入全量通过
试点期（2–4 周）─────────────→ 在线埋点周报 + 每周用户回访
上线后常态化 ────────────────→ 在线指标看板持续监控；
                              每月抽检 50 条引用做忠实度人工复核；
                              每季度重跑对抗注入
```

- **评测集维护**：评测集是活资产——试点中发现的每一例真实错误（误记、误唤起、漏出处）都必须回填为新的评测用例，评测集随产品演进只增不减。
- **判定责任**：忠实度、相关性这类语义判定采用"LLM-as-judge 初筛 + 人工复核争议项"，但**达标线的最终判定权在人工**，不用模型给模型发合格证。

---

## 五、不达标的处置（评测方案的另一半）

| 跌破的指标 | 处置 |
|---|---|
| 写入忠实度 / 出处正确率 | **立即止血**：提高显式确认比例（全自动档临时关闭），修复后重跑门禁 |
| 冲突检测漏检 | **立即止血**：同上；冲突漏检等同静默污染记忆库 |
| 唤起误报率超标 | 不止血但降频：提高唤起阈值、更多唤起降级为弱展示 |
| 写入召回率不达标 | 不阻断，列为下迭代重点；漏记的体感是"退回普通助手"，不是信任事故 |
| 纠错时长/影响面提示不达标 | 阻断扩张：纠错闭环是信任的最后一道防线，未达标前不扩大用户量 |

**一条总原则**：准确性与可溯源性的问题一律熔断（因为它们产生复利式负资产）；相关性与覆盖率的问题迭代优化（因为它们只是价值不足，不产生负资产）。

---

## 六、与前三份交付物的关系

- **任务 1** 给出成立标准（"用户敢不敢不核对"）与量化验收线 → 本方案把它落成可执行的指标、方法与门禁；
- **任务 2** 的每个机制（置信度分流、出处、版本、影响面、隔离）都对应本方案中至少一个评测项——机制设计时可评测，是设计时就埋下的；
- **任务 3** 原型中的演示链路（记录→调用→纠正）可作为离线评测集前 3 个剧本的原型。

**一句话总结**：这个产品的评测不是"上线前考一次试"，而是**一套永远在线的信任仪表**——因为记忆型产品的资产是积累出来的，积累的资格要靠每一次引用都经得起检验来维持。
